source 'https://rubygems.org'
source 'https://rails-assets.org'
ruby '2.0.0'

gem 'bourbon' # Bourbon for Sass
gem 'jbuilder', '~> 2.0'
gem 'jquery-rails' # JQuery
gem 'pg' # database
gem 'rails', '4.1.2' # Rails itself
gem 'sass-rails', '~> 4.0.3' # Sass stylesheets
gem 'turbolinks'
gem 'uglifier', '>= 1.3.0' # compress JS

group :development, :test do
  gem 'factory_girl_rails', '~> 4.2.1'
  gem 'json_spec'
  gem 'rake'
end

group :development, :production do
  gem 'rails_12factor'
end
